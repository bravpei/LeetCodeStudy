package moon.star

import scala.collection.mutable.ArrayBuffer

object Driver {
  def main(args: Array[String]): Unit = {
   println(new FirstUniqChar().firstUniqChar("loveleetcode"))
    var s="sadf"
  }
}
