package moon.star

import scala.collection.mutable.ArrayBuffer

class Intersect {
  def intersect(nums1:Array[Int],nums2:Array[Int]):Array[Int]={
    var nums3=nums2.toBuffer
    var nums:ArrayBuffer[Int]=new ArrayBuffer[Int]()
    var boo=true
    for(i<-nums1.indices){

    }
    nums.toArray
  }
}
